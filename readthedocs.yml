version: 2

sphinx:
  builder: html
  configuration: docs/src/conf.py
  fail_on_warning: false

formats: all

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
  system_packages: false
